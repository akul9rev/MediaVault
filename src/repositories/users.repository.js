import pool from '../config/db.js';

/**
 * Users Repository
 * Direct MySQL database operations for retrieving profile, setting values.
 */
class UsersRepository {
  // Database queries will be added in future stages
}

export default new UsersRepository();
