export const API_CONFIG = {
  // Loopback IP pointing to localhost from Android Emulator
  // Change to your machine's LAN IP (e.g., http://192.168.X.X:5000/api/v1) for physical device testing
  baseUrl: 'https://mediavault-c0ts.onrender.com/api/v1',
  timeout: 10000
};

export default API_CONFIG;
